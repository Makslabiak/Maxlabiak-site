{
  "name": "dars-site-audit",
  "version": "1.0.0",
  "private": true,
  "description": "Сайт Dars Studio + backend экспресс-аудита сайтов клиентов",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch server/server.js",
    "start": "node server/server.js",
    "test": "node --test \"tests/**/*.test.js\"",
    "playwright:install": "playwright install chromium"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0",
    "playwright": "^1.49.1",
    "zod": "^3.24.1"
  }
}
