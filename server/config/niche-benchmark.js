/**
 * niche-benchmark.js — сравнение с типичным сайтом ремонтной компании.
 *
 * ЗАЧЕМ ЭТОТ ФАЙЛ ОТДЕЛЬНЫЙ И ВЫКЛЮЧЕН ПО УМОЛЧАНИЮ.
 *
 * Сравнение с конкурентами продаёт сильнее абстрактного балла: «ваш сайт
 * открывается за 4,8 сек, у трёх конкурентов в вашем городе — 1,9–2,6»
 * невозможно опровергнуть, и это работает только пока цифры настоящие.
 * Одна выдуманная цифра, которую посетитель или конкурент проверит сам, —
 * и доверие ко всему инструменту сгорает разом, включая реальные находки.
 *
 * Поэтому: ENABLED = false, пока сюда не внесены цифры, полученные
 * реальным прогоном конкурентов через этот же инструмент.
 *
 * КАК ЗАПОЛНИТЬ:
 *   1. Возьми 15–20 сайтов ремонтных компаний — конкурентов твоих клиентов
 *      в разных городах (можно обычным запросом POST /api/audit,
 *      AUDIT_MOCK_MODE=false).
 *   2. Впиши в SAMPLE_SIZE реальное количество прогнанных сайтов.
 *   3. Для каждой метрики посчитай среднее или медиану и впиши в value.
 *   4. Для долевых признаков (calculator, warrantyTerm и т.д.) посчитай
 *      процент сайтов, где признак найден.
 *   5. Поставь ENABLED = true.
 *   6. Раз в несколько месяцев данные стоит освежать — ниша меняется.
 *
 * Дата последнего сбора — в UPDATED_AT, чтобы не запустить с устаревшими
 * цифрами через полгода, забыв об этом.
 */

export const ENABLED = false;

export const SAMPLE_SIZE = 0;
export const UPDATED_AT = null; // 'YYYY-MM-DD'

/**
 * Метрики скорости — типичное значение и цифра в displayValue
 * ровно в том же формате, что показывает наш собственный отчёт.
 */
export const SPEED_BENCHMARK = {
  lcp: { value: null, displayValue: null },        // например: { value: 2600, displayValue: '2,6 сек.' }
  performance: { value: null, displayValue: null }  // например: { value: 71, displayValue: '71 / 100' }
};

/**
 * Доля сайтов в выборке, у которых признак присутствует.
 * Ключи совпадают с тем, что уже проверяет html-audit.service —
 * специально, чтобы сравнение считалось автоматически.
 */
export const FEATURE_BENCHMARK = {
  calculator: null,      // например: 0.4  →  «есть у 40% конкурентов»
  priceNumbers: null,
  warrantyTerm: null,
  casesEnough: null,
  reviewsEnough: null,
  instantAnswer: null    // чат, мессенджер или обратный звонок
};
