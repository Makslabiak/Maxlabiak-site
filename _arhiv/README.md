# Архив

Здесь то, что в работе сайта не участвует. Можно удалить целиком,
если уверен, что откатываться не понадобится.

| Что | Зачем лежит |
|---|---|
| `prototip-figma-react/` | Исходный прототип из Figma Make (React + Tailwind). Из него взяты структура и тексты всех блоков. Сам сайт на него не ссылается |
| `index-canvas-version.backup.html` | Версия сайта на технике «канва + transform: scale()» — от неё отказались в пользу обычной flow-вёрстки, потому что канва ломает `position: fixed` (шапка) и `position: sticky` (заголовок в блоке «Проблемы») |
| `index-hero-only.backup.html` | Самая первая версия: только первый экран, до сборки остальных блоков |
